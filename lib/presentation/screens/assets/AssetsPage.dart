import 'package:flutter/material.dart';

class AssetsPage extends StatelessWidget {
  const AssetsPage({super.key});

  @override
  Widget build(BuildContext settingsContext) {
    return const Scaffold(
      body: Center(child: Text('Assets')),
    );
  }
}
